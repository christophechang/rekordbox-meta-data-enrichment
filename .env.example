# LLM disambiguation — provider cascade
# At least one required; MiniMax is preferred (flat-fee subscription)
MINIMAX_API_KEY=
GROQ_API_KEY=
GEMINI_API_KEY=

# Optional: Discogs authenticated lookups (60/min unauth, 25/min auth)
DISCOGS_TOKEN=
